# Elixir / Phoenix
/_build/
/deps/
/cover/
/doc/
/.fetch
erl_crash.dump
*.ez
*.beam
/config/*.secret.exs
.elixir_ls/
.lexical/

# Phoenix assets
/priv/static/assets/
/priv/static/cache_manifest.json
node_modules/

# Database files (libSQL/SQLite) — the store lives on a volume, never in git
*.db
*.db-*
*.sqlite3
*.sqlite3-*

# Litestream
.litestream/

# Secrets & env — keypair, R2 creds, email API key, OAuth client secrets
.env
.env.*
!.env.example
*.pem
*.key

# OS / editor
.DS_Store
